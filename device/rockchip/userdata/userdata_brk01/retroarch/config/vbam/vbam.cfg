# VBA-M Core Override Configuration
# Applies to GB, GBC, and GBA games

# Use core-provided aspect ratio
aspect_ratio_index = "22"

# Enable integer scaling for pixel-perfect display
video_scale_integer = "true"

# Force aspect ratio (use core's reported ratio)
video_force_aspect = "true"

# Disable smoothing for crisp pixels
video_smooth = "false"
