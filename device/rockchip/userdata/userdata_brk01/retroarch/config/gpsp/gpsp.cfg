# gpSP Core Override Configuration
# GBA-specific settings (3:2 aspect ratio)

# Use core-provided aspect ratio (3:2 for GBA)
aspect_ratio_index = "22"

# Enable integer scaling for pixel-perfect display
video_scale_integer = "true"

# Force aspect ratio
video_force_aspect = "true"

# Disable smoothing for crisp pixels
video_smooth = "false"
